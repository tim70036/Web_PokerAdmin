const
    serviceAgent    = require('./serviceAgent'),
    headAgent       = require('./headAgent'),
    agent           = require('./agent'),
    member          = require('./member');

module.exports = {
    serviceAgent    : serviceAgent,
    headAgent       : headAgent,
    agent           : agent,
    member          : member,
};